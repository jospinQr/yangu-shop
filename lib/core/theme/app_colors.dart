import 'package:flutter/material.dart';
class AppColors {
  static const beige = Color(0xFFF3E5C6);
  static const secondary = Color(0xFFD9C9A3);     // Beige plus sombre / Sable
  static const secondaryDark = Color(0xFF8B7355); // Brun moyen
  static const accent = Color(0xFFC07B6B);        // Terracotta (optionnel, pour le punch)
  static const deepBrown = Color(0xFF4E342E);
}
